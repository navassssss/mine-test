{
  "name": "kimi-workspace",
  "version": "1.0.0",
  "description": "Root workspace containing Kindle client and API server",
  "scripts": {
    "start-api": "npm start --prefix api",
    "start-kindle": "npm start --prefix kindle",
    "dev-api": "npm run dev --prefix api",
    "dev-kindle": "npm run dev --prefix kindle"
  }
}
